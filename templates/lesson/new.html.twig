{% extends 'base.html.twig' %}

{% block title %}Новый урок{% endblock %}

{% block body %}
    <h1>Новый урок</h1>
    <h4>Курс: <a class="link-underline link-underline-opacity-0" href="{{ path('app_course_show', {id: lesson.course.id}) }}">{{ lesson.course.name }}</a></h4>

    {{ include('lesson/_form.html.twig') }}

{% endblock %}
