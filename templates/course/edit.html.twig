{% extends 'base.html.twig' %}

{% block title %}Редактирование курса "{{ course.name }}"{% endblock %}

{% block body %}
    <h1>Редактирование курса</h1>

    {{ include('course/_form.html.twig', {'button_label': 'Сохранить изменения'}) }}

    <a href="{{ path('app_course_index') }}" class="btn btn-outline-secondary mt-4">К списку курсов</a>
    <hr>
    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
